import { LucideIcon } from 'lucide-react';

/**
 * Common shared types used across different modules
 */

export interface ColorConfig {
  red: string;
  blue: string;
  green: string;
  purple: string;
  amber?: string;
  gray?: string;
} 